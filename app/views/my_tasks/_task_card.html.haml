.column.is-one-third
  .card
    .card-content
      %h5 Tipo: #{I18n.t("task.category.#{task.category}")}
      %h6.text-truncate #{task.description}
      %p Fecha de entrega: #{I18n.l(task.deadline, format: :full)}
      %p
        %b Campaña:
        #{task.post.node.network.campaign.name}
    .card-footer.justify-content-end.p-10px
      = link_to 'Ver', my_task_path(task), class: 'button is-light'
      = link_to 'Trabajar en tarea', edit_my_task_path(task), class: 'button is-info ml-5px'


