= link_to 'Volver a publicaciones de la campaña', campaign_posts_path(@post.node.campaign_id), class: 'btn btn-primary'

%h5 #{@post.title}
%p Contenido: #{@post.body}

= link_to 'Agregar tarea', new_post_task_path(@post), class: 'btn btn-primary'

%h4 Tareas

= render partial: 'tasks/task_card', collection: @post.tasks, as: :task
