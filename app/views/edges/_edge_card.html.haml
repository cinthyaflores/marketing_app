.card.w-300px
  .card-body
    %h5.card-title Identificador: #{edge.id}
    %p.card-text De nodo: #{edge.from_id}
    %p.card-text Al nodo: #{edge.to_id}
    = link_to 'Eliminar', network_edge_path(id: edge.id, network_id: edge.network_id), class: 'btn btn-danger', method: :delete
    = link_to 'Editar', edit_network_edge_path(id: edge.id, network_id: edge.network_id), class: 'btn btn-secondary'
